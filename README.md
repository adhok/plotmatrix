# plotmatrix
Simple R library to plot matrices

The library is a student project to properly plot a matrix. The matrix can be in the form of a dataframe or a matrix

To install please run
```devtools::install_github("adhok/plotmatrix")```
